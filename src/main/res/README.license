v1.2.3 round-33 (2026.7.20): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - All round-33 changes are in src/main/java/com/Regalia/ (Java files) +
    NOTICE + Manual. The res/ tree (mipmap-*, values/strings.xml, xml/*.xml)
    is unchanged.

v1.2.3 round-32 (2026.7.20): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - All round-32 changes are in src/main/assets/chess.src/ (JS modules)
    and src/main/java/com/Regalia/ (Java files) — see those README.license
    files. The res/ tree (mipmap-*, values/strings.xml, xml/*.xml) is
    unchanged.

v1.2.3 round-31 (2026.7.20): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - All round-31 changes are in src/main/assets/chess.src/ (JS modules)
    and src/main/java/com/Regalia/ (Java files) — see those README.license
    files. The res/ tree (mipmap-*, values/strings.xml, xml/*.xml) is
    unchanged.

v1.2.3 round-30 (2026.7.19) — First-principles per-file review + robustness/perf optimizations:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - No resource files changed this round.

v1.2.3 round-29 (2026.7.19) — PR52 SonarCloud + CodeRabbit review fixes:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - No resource files changed this round.
  - xml/ summary updated: noted that backup_rules.xml and
    data_extraction_rules.xml are NOT currently referenced by AndroidManifest
    (allowBackup="false" makes them dead config since round-13). The XML
    files are retained in res/xml/ for reference in case allowBackup is
    ever set to true in the future.

v1.2.3 round-28 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — audit round).

v1.2.3 round-27 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — review round).
  - The round-27 change is limited to src/main/assets/chess.src/ and the rebuilt chess.html.

v1.2.3 round-26 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - The round-26 change is limited to src/main/assets/chess.src/ and the rebuilt chess.html.

v1.2.3 round-25 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — style + feature round).
  - The round-25 change is limited to src/main/assets/chess.src/ and the rebuilt chess.html.

v1.2.3 round-24 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — refactor round).
  - The round-24 change is limited to src/main/assets/chess.src/ui.js (God
    Function split) and src/main/java/com/Regalia/README.license (StatsActivity
    license label correction).

v1.2.3 round-23 (2026.7.19): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - The round-23 changes are limited to src/main/java/com/Regalia/
    (HapticManager.java PWLE reflection removal — Q11+Q17),
    src/main/assets/chess.src/ (PR51 JS fixes Q1-Q7, Q10),
    src/main/assets/stats.html (Q7+Q9), src/main/AndroidManifest.xml
    (Q13 comment fix), and the rebuilt chess.html plus top-level docs.

v1.2.3 round-22 (2026.7.18): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - The round-22 change is limited to src/main/assets/chess.src/ (ui.js,
    ai-bridge.js eval-perspective fixes) plus the rebuilt chess.html and the
    top-level docs / Manual.

v1.2.3 round-21 (2026.7.17): no changes in this directory.
  - Version: versionCode=123, versionName="1.2.3" (unchanged — fix round).
  - The round-21 change is limited to src/main/assets/chess.src/ai-bridge.js
    (per-color gated king signal in _needsShredderFEN) plus rebuilt chess.html
    and the top-level docs/Manual.

v1.2.3 round-20 (2026.7.17) — 4 known leftovers + 3rd review report + hint relocation:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — hardening round).
  - No changes to any resource files this round (strings.xml, icons,
    network_security_config.xml, backup_rules.xml, data_extraction_rules.xml).
  - License classification: all resources remain AGPL v3 (original works).

v1.2.3 round-19 (2026.7.17) — 2 bilingual UX hints + log-injection fix:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — feature + fix round).
  - No changes to any resource files this round (strings.xml, icons,
    network_security_config.xml, backup_rules.xml, data_extraction_rules.xml).
  - License classification: all resources remain AGPL v3 (original works).

v1.2.3 round-18 (2026.7.17) — Chess960 PGN [FEN] bug fix + 7-agent line-by-line review:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — bug-fix round).
  - No changes to any resource files this round (strings.xml, icons,
    network_security_config.xml, backup_rules.xml, data_extraction_rules.xml).
  - License classification: all resources remain AGPL v3 (original works).

v1.2.3 round-13 (2026.7.16) — CMake re-enablement + first-principles code review:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — bug-fix round).
  - network_security_config.xml (AGPL v3): P3 — corrected ISRG Root X2 expiry
    date in comment (was 2035-09-06, actual is 2035-09-17). The pin itself
    was already correct.
  - strings.xml, backup_rules.xml, data_extraction_rules.xml: no changes.
    (Note: backup_rules.xml and data_extraction_rules.xml are no longer
    referenced by AndroidManifest.xml — the fullBackupContent and
    dataExtractionRules attributes were removed because allowBackup="false"
    makes them dead config. The XML files are retained in res/xml/ for
    reference in case allowBackup is ever set to true in the future.)
  - All mipmap-* icon resources: no changes.
  - License classification: all resources remain AGPL v3 (original works).

v1.2.3 round-12 (2026.7.16) — SonarCloud bug fixes + first-principles review:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — bug-fix round).
  - No changes to any resource files this round (strings.xml, icons,
    network_security_config.xml, backup_rules.xml, data_extraction_rules.xml).
  - License classification: all resources remain AGPL v3 (original works).

v1.2.3 optimization pass (2026.7.16) — first-principles line-by-line review:
  - Version: versionCode=123, versionName="1.2.3" (unchanged — no version bump).
  - network_security_config.xml (AGPL v3): Corrected "minSdk 21" comment to
    "minSdk 23" to match build.gradle. No XML change.
  - strings.xml, backup_rules.xml, data_extraction_rules.xml: no changes.
  - All mipmap-* icon resources: no changes.
  - License classification: unchanged (all AGPL v3).

v1.2.3 (2026.7.16) — Round 17/18 review fixes + P0 JS error fix + Toast UX:
  - Version: versionCode=123, versionName="1.2.3" (version bump from v1.2.2).
  - strings.xml (AGPL v3): app_name updated from "Regalia v1.2.2" to
    "Regalia v1.2.3".
  - All mipmap-* icon resources: no changes.
  - xml/network_security_config.xml, xml/backup_rules.xml,
    xml/data_extraction_rules.xml: no changes.
  - License classification: all files in this directory retain their
    existing AGPL v3 classification.

v1.2.2 (2026.7.14) — Comprehensive audit-report non-false-positive fix + version bump:
  - Version: versionCode=122, versionName="1.2.2" (version bump from v1.2.1).
  - strings.xml (AGPL v3): app_name updated from "Regalia v1.2.1" to
    "Regalia v1.2.2". No other resource files modified.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-16 (2026.7.14) — User-reported UX clarity + audit-report non-false-positive defect fixes:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource files modified in this pass. All round-16 changes were
    in chess.src/ JS modules, stats.html, and the rebuilt chess.html. See
    chess.src/README.license for details.
  - License classification: all files in this directory retain their
    existing classification.
v1.2.1 round-15 (2026.7.14) — Final perfection: comprehensive PDF-guided audit:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource files modified in this pass. All round-15 changes were
    in chess.src/ui.js and stats.html. See chess.src/README.license for
    details.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-14 (2026.7.14) — S3776 _renderDialogs extraction + S2703 typeof audit:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource files modified in this pass. All round-14 changes were
    in chess.src/ JS modules. See chess.src/README.license for details.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-13 (2026.7.14) — S3776 cognitive complexity: renderInternal God Function refactor:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource files modified in this pass. All round-13 changes were
    in chess.src/ui.js. See chess.src/README.license for details.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-12 (2026.7.14) — SonarCloud PR #43 bugs + code smells cleanup:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource files modified in this pass. All round-12 changes were
    in chess.src/ (JS modules) and index.html.tpl (CSS). See
    chess.src/README.license for details.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-11 (2026.7.14) — 2 user-reported bugs + review-report defects + first-principles optimization:
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No source files in this directory modified in this pass. All changes
    were in chess.src/ (JS modules), java/com/Regalia/ (FileIoHelper.java),
    and the rebuilt chess.html. See those directories' README.license files
    for details.
  - License classification: all files in this directory retain their
    existing classification.

v1.2.1 round-10 regression test + first-principles optimization (2026.7.14):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - Regression test of all review-D/E/F fixes — all verified intact. No
    source changes in this directory this pass; see java/com/Regalia/README.license
    for the 4 first-principles optimization items (StockfishNative
    PROCESS_DESTROY_GRACE_MS extraction, StatsActivity redundant Uri.parse
    removal, StabilizationHelper comment direction fix, proguard-rules.pro
    section-3 comment fix).
  - License classification: all files in this directory retain their existing
    classification.

v1.2.1 (2026.7.14, round-10 continuation — secondary review-E items):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource changes in this continuation pass.
  - All resources retain existing license classification.

v1.2.1 (2026.7.14, tenth-pass refinement — round-10 review):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource changes in this pass.
  - All resources retain existing license classification.

v1.2.1 (2026.7.13, ninth-pass refinement — round-9 review):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version
    refinement).
  - No resource changes in this pass.
  - All resources retain existing license classification.

v1.2.1 (2026.7.13, eighth-pass refinement — round-8 review):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version refinement).
  - No resource changes in this pass. The round-8 fix is entirely contained
    in src/main/assets/chess.src/state-store.js (AGPL v3) — a TDZ (Temporal
    Dead Zone) bug fix for the `const DEEP_CLONE_MAX_DEPTH` declaration
    position.
  - All resources retain existing license classification.

v1.2.1 (2026.7.13, eighth-pass refinement — round-8 review):
  - Version: versionCode=121, versionName="1.2.1" (unchanged — same-version refinement).
  - No resource changes in this pass. The round-8 fix is entirely contained
    in src/main/assets/chess.src/state-store.js (AGPL v3) — a TDZ (Temporal
    Dead Zone) bug fix for the `const DEEP_CLONE_MAX_DEPTH` declaration
    position.
  - All resources retain existing license classification.

This directory contains Android resource files used by the Regalia app.

NOTE on README.license placement:
  Android's AAPT2 resource merger strictly requires every file directly
  inside res/ subfolders to end in .xml, .png, .webp, or .9.png. Plain-text
  files like README.license fail this validation, so per-subfolder
  README.license files cannot live inside res/values/, res/xml/, or
  res/mipmap-*/. Instead, this single README.license at the res/ root
  documents the license for ALL res/ subfolders. The same license
  information is also mirrored in src/main/README.license.

Subdirectory license summary:
  - values/: AGPL v3 (original)
      strings.xml — application name ("Regalia v1.1.1") and string
      resources. Contains app_name, dialog titles, button labels,
      toast messages, ECO family names, and all user-facing strings
      in both Chinese and English (selected at runtime via _lang).
      (v1.0.8 PHASE 30: updated from stale "Regalia v1.0.3" reference.)
  - xml/: AGPL v3 (original)
      network_security_config.xml — TLS 1.2+ enforcement + cleartext
      block for HTTPS connections to the Lichess Tablebase API.
      The config disables cleartext traffic entirely and trusts the
      system CA store; TlsSecurityHelper.java additionally enforces
      TLS 1.2+ via SSLSocketFactory on API 21-27 where the system
      default might negotiate TLS 1.0/1.1.
      v1.0.3: no changes.
      v1.0.4 Round-5 Rev17: backup_rules.xml and data_extraction_rules.xml
      added. backup_rules.xml (<full-backup-content>) declares the
      sharedpref domain and app_webview/Local Storage/ directory as
      user data to be preserved during cloud backup.
      data_extraction_rules.xml (<data-extraction-rules>, Android 12+)
      mirrors this for cloud-backup and device-transfer scenarios.
      v1.2.3 round-13 NOTE: AndroidManifest.xml's android:fullBackupContent
      and android:dataExtractionRules attributes were REMOVED (because
      allowBackup="false" makes them dead config). The XML files are
      retained in res/xml/ for reference in case allowBackup is ever set
      to true in the future, but they are NOT currently referenced by
      the manifest.
  - mipmap-hdpi/ ... mipmap-xxxhdpi/: AGPL v3 (AI-generated assets)
      App launcher icons (ic_launcher, ic_launcher_round,
      ic_launcher_foreground) in 5 density buckets (hdpi, mdpi, xhdpi,
      xxhdpi, xxxhdpi). All icons are AI-generated and licensed under
      AGPL v3 — NOT derived from DroidFish or Stockfish. The ic_launcher
      foreground depicts a gold crown on a dark rounded-square
      background, matching the in-app ♔ header logo.
      v1.0.3: no changes.

Overall, all resources in this directory tree are original works licensed
under AGPL v3. No DroidFish-derived or Stockfish-derived resources are
present here.

See the root-level LICENSE-AGPL v3, LICENSE-GPL v3, and NOTICE files
for full license texts and attribution details.

v1.0.3 patch: no changes to res/ files (strings.xml, network_security_config.xml,
and all mipmap launcher icons are unchanged from v1.0.3 baseline).

v1.0.4 Round-5 Rev17: added res/xml/backup_rules.xml and
res/xml/data_extraction_rules.xml (both AGPL v3, original configuration
declaring SharedPreferences and WebView Local Storage as user data).
network_security_config.xml and all mipmap icons unchanged.


v1.0.4 Round-5 Rev23: no changes to res/ files (backup_rules.xml,
data_extraction_rules.xml, network_security_config.xml, strings.xml, and all
mipmap launcher icons are unchanged from the Rev22 baseline).

v1.0.4 Round-5 Rev24: no changes to res/ files. All Rev24 changes are in
the JS layer (chess.src/) and the Stockfish engine binary.

v1.0.4 Round-5 Rev25: no changes to res/ files.

v1.0.4 Round-5 Rev26: no changes to res/ files.

v1.0.4 Round-5 Rev27: no changes to res/ files.

v1.0.4 Round-5 Rev28: no changes to res/ files.

v1.0.4 Round-5 Rev29: no changes to res/ files. All Rev29 changes are in
the JS layer (chess.src/tablebase.js, chess.src/ai-bridge.js, stats.html).

v1.0.4 Round-5 Rev30: no changes to res/ files. All Rev30 changes are in
the Java layer (ChessWebViewClient.java) and JS layer (chess.src/ui.js,
chess.src/chess960.js).

v1.0.4 Round-5 Rev31: no changes to res/ files. All Rev31 changes are in
the Java layer (StockfishNative.java) and JS layer (chess.src/ai-bridge.js,
chess.src/tablebase.js, chess.src/pgn-standard.js, chess.src/game-logic.js,
stats.html).

v1.0.4 Round-5 Rev32: no changes to res/ files. All Rev32 changes are in
the Java layer (StockfishNative.java) and JS layer (chess.src/
game-logic.js).

v1.0.4 Round-5 Rev33: no changes to res/ files. All Rev33 changes are in
the Java layer (StockfishNative.java) and JS layer (chess.src/
ai-bridge.js, chess.src/ui.js).

v1.0.4 Round-5 Rev34: no changes to res/ files. All Rev34 changes are in
the JS layer (chess.src/chess960.js, stats.html).

v1.0.4 Round-5 Rev35: no changes to res/ files. All Rev35 changes are in
the Java layer (StockfishNative.java) and JS layer (chess.src/
ai-bridge.js, chess.src/game-logic.js, chess.src/ui.js).

v1.0.4 Round-5 Rev36: no changes to res/ files. All Rev36 changes are in
the Java layer (StockfishNative.java) and JS layer (chess.src/
ai-bridge.js, chess.src/ui.js, stats.html).

v1.0.4 Round-5 Rev37: no changes to res/ files. All Rev37 changes are in
the JS layer (chess.src/ui.js).

v1.0.4 Round-5 Rev38: no changes to res/ files. All Rev38 changes are in
the JS layer (chess.src/ui.js, chess.src/index.html.tpl).

v1.0.4 Round-5 Rev39: no changes to res/ files. All Rev39 changes are in
the JS layer (chess.src/ui.js).

v1.0.4 Round-5 Rev40: no changes to res/ files. All Rev40 changes are in
the JS layer (chess.src/ui.js).

v1.0.4 Round-5 Rev41: no changes to res/ files. All Rev41 changes are in
the JS layer (chess.src/ui.js, stats.html).

v1.0.4 Round-5 Rev42: no changes to res/ files. All Rev42 changes are in
the JS layer (chess.src/ui.js).

v1.0.4 Round-5 Rev43: no changes to res/ files. All Rev43 changes are in
stats.html.

v1.0.4 Round-5 Rev44: no changes to res/ files. All Rev44 changes are in
the JS layer (chess.src/ai-bridge.js, chess.src/worker-pool.js, stats.html).

v1.0.4 Round-5 Rev45: no changes to res/ files. All Rev45 changes are in
the JS layer (chess.src/game-logic.js, chess.src/ai-bridge.js).

v1.0.4 Round-5 Rev46: no changes to res/ files. All Rev46 changes are in
the JS layer (chess.src/ui.js, chess.src/game-logic.js, chess.src/ai-bridge.js).

v1.0.4 Round-5 Rev47: no changes to res/ files. All Rev47 changes are in
the JS layer.

v1.0.4 Round-5 Rev48: no changes to res/ files. All Rev48 changes are in
the JS/HTML layer (chess.src/ + stats.html).

v1.0.5 Round-6 Rev49: no changes to res/ files. All Rev49 changes are in
the Java layer and the JS/HTML layer.

v1.0.5 Round-6 Rev50: no changes to res/ files.

v1.0.5 Round-6 Rev51 changes:
  - values/strings.xml: app_name "Regalia v1.0.4" → "Regalia v1.0.5".
    (This is the APP launcher icon name shown on the system home screen.)

v1.0.5 Round-6 Rev52: no changes to res/ files.

v1.0.5 Round-6 Rev53: no changes to res/ files.

v1.0.5 Round-6 Rev54 through Rev68: no changes to res/ files. All changes in
this range were in the Java layer (StabilizationHelper.java, MainActivity.java,
StockfishNative.java, StatsActivity.java) and/or the JS layer (chess.src/
ui.js, chess960.js, game-logic.js, tablebase.js, worker-pool.js, pgn-standard.js,
ai-bridge.js, index.html.tpl). strings.xml, network_security_config.xml,
backup_rules.xml, data_extraction_rules.xml, and all mipmap launcher icons
remain unchanged from the Rev53 baseline.

v1.0.8 (2026.6.30): no changes to res/ files. All v1.0.8 changes were in the
JS layer (chess.src/) and stats.html. strings.xml (app_name="Regalia v1.0.8"),
network_security_config.xml, backup_rules.xml, data_extraction_rules.xml, and
all mipmap launcher icons remain unchanged from the v1.0.7 baseline.

v1.0.8 Phase 23 (2026.7.1): No changes to res/ files.

v1.0.8 Phase 24 (2026.7.1): No changes to res/ files. strings.xml
(app_name="Regalia v1.0.8"), network_security_config.xml, backup_rules.xml,
data_extraction_rules.xml, and all mipmap launcher icons remain unchanged
from the v1.0.7 baseline.

v1.0.8 Phase 25 (2026.7.1): No changes to res/ files.

v1.0.8 Phase 26 (2026.7.1): No changes to res/ files.

v1.0.8 Phase 27 (2026.7.1): No changes to res/ files.

v1.0.8 Phase 28 (2026.7.1): No changes to res/ files.

v1.0.8 Phase 29 (2026.7.1):
  - No changes to res/ files.

v1.0.8 Phase 30 (2026.7.1):
  - strings.xml (AGPL v3): full AGPL v3 boilerplate header added.
  - network_security_config.xml (AGPL v3): full AGPL v3 boilerplate
    header added.
  - backup_rules.xml (AGPL v3): full AGPL v3 boilerplate header added.
  - data_extraction_rules.xml (AGPL v3): full AGPL v3 boilerplate
    header added.
  - Fixed stale strings.xml version reference (v1.0.3 → v1.0.8).

v1.0.8 Phase 43 (2026.7.1):
  - No changes to res/ files.

v1.0.8 Phase 42 (2026.7.1):
  - No changes to res/ files.

v1.0.8 Phase 41 (2026.7.1):
  - No changes to res/ files.


v1.1.0 Phase 57 (2026.7.4):
  - No changes to Android resources. Phase 57 fixes are in JavaScript (ui.js)
    only — pure DOM/layout and visual-annotation cache fixes.

v1.1.0 Phase 56 (2026.7.4):
  - No changes to Android resources. Phase 56 fixes are in JavaScript (ui.js,
    ai-bridge.js) and Java (StockfishNative.java).

v1.1.0 Phase 55 (2026.7.4):
  - No changes to Android resources in this phase. Phase 55 fixes a Chess960
    castling bug in stats.html (JavaScript) and game-logic.js (JavaScript).

v1.1.0 Phase 54 (2026.7.4):
  - ui.js + index.html.tpl: custom slider (track/fill/thumb divs + transparent
    native input overlay) for pixel-perfect alignment with eval trend chart.
    Slider wrapper CSS identical to chart container. Thumb center via CSS
    calc(3px + ratio * (100% - 6px)).
  - ui.js: move-list scroll-into-view fix (only scroll if not visible,
    _lastReviewStepScrolled moved inside rAF callback).
  - ui.js: executeMove async-callback try-catch.
  - ui.js: ChessAudioEngine partial-init field reset + setEnabled/setVolume
    null-guard.
  - ui.js: render() retry-loop guard (_animRetryCount, max 10).
  - ai-bridge.js: heartbeat timestamp in all engine callbacks (onEngineProgress,
    onBestMove, onHintMove, onPonderProgress).
  - ai-bridge.js: MultiPV secondary-variation divergence fix (distinguish
    alternative vs continuation).
  - tablebase.js: PGN cascade-skip threshold 5 → max(15, len*0.1).
  All changes in GPL-v3-licensed files.


v1.1.2 Phase 72 (2026.7.12, same-version revision):
  - Version remains versionCode=112, versionName="1.1.2".
  - No changes to res/ files in Phase 72.
  - License classification unchanged.

v1.1.2 Phase 71 (2026.7.11, same-version revision):
  - Version remains versionCode=112, versionName="1.1.2".
  - No changes to res/ files in Phase 71.
  - License classification unchanged.

v1.1.2 Phase 70 (2026.7.10, same-version revision):
  - Version remains versionCode=112, versionName="1.1.2".
  - No changes to res/ files in Phase 70.
  - License classification unchanged.

v1.1.2 Phase 69 (2026.7.9, same-version revision):
  - Version remains versionCode=112, versionName="1.1.2".
  - No changes to res/ files in Phase 69.
  - License classification unchanged.

v1.1.2 Phase 68 (2026.7.8, same-version revision):
  - Version remains versionCode=112, versionName="1.1.2".
  - No changes to res/ files in Phase 68. strings.xml, xml/*.xml, mipmap-*/
    are all unchanged.
  - License classification unchanged.

v1.1.2 Phase 67 (2026.7.7):
  - Version bumped to versionCode=112, versionName="1.1.2".
  - values/strings.xml (AGPL v3): app_name "Regalia v1.1.1" → "Regalia v1.1.2".
  - xml/backup_rules.xml, xml/data_extraction_rules.xml, xml/network_security_config.xml
    (all AGPL v3): no changes. backup_rules.xml continues to protect
    SharedPreferences, WebView LocalStorage, eval_cache.json, and pgn_cache/
    directory (the allowBackup="true" decision is intentional — preserves
    user's PGN cache and engine settings across device transfers).
  - No new resources introduced.

v1.1.1 Phase 66 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - src/main/assets/chess.src/ui.js: Bug fix — _savePGNYes now polls
    _pgnExportDialogActive before executing pending new-game action (was
    using fixed 200ms setTimeout, could fire while annotation dialog open);
    removed 2 stale console.log calls (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ai-bridge.js: openStatsPage now explicitly
    passes includeAnnotations=true to _buildPGNString (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 65 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - src/main/assets/chess.src/ai-bridge.js: _showPGNExportAnnotationDialog
    now supports Android back-button (via _pgnExportDialogActive/
    _pgnExportDialogDismiss globals) and explicit haptic feedback on all
    buttons (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ui.js: _resetGameUIState now clears
    _preReviewSnapshot, setupHistory/setupRedoStack, and 7 dialog visibility
    flags (showNewGameDialog/showAboutPage/showImportDialog/showEngineConfig/
    showPGNCacheManager/showSavePGNPrompt/showResignConfirm/
    _pendingActionAfterSave); handleBackPress checks _pgnExportDialogActive
    first (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 64 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - TRUE ROOT CAUSE FIX: reviewStates (from previous game's review session)
    was not cleared in _resetGameUIState(), causing _computeAndCacheVisualAnnotations
    to use stale board states from the old game at the same move index —
    producing annotations identical to the old game at that step.
    Fix: _resetGameUIState now clears reviewStates/reviewMode/reviewStep/
    reviewBaseState; _computeAndCacheVisualAnnotations gates reviewStates
    shortcut on reviewMode===true (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/game-logic.js: updated export dialog i18n text
    (💾 emoji, "特殊注释"/"special annotations" terminology) (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 63 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - Full-chain audit of all [%csl]/[%cal] visual annotation code paths
    confirmed Phase 62 fix is correct.
  - src/main/assets/chess.src/ai-bridge.js: _buildPGNString now accepts
    includeAnnotations parameter (default true); when false, all annotation-
    type tags ([%eval], every-5-moves desc, [%csl]/[%cal]) are omitted;
    new _showPGNExportAnnotationDialog function; copyMoveHistory and
    exportPGNToFile now show the dialog before exporting (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ui.js: _pgnCacheSaveCurrent now shows the
    annotation dialog; extracted _pgnCacheSaveCurrentImpl (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/game-logic.js: new i18n keys for export dialog
    (pgn_export_include_annotations_title/msg/yes/no) (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 62 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - src/main/assets/chess.src/ui.js: _visualAnnotationsCache entries now
    include imported boolean flag; _computeAndCacheVisualAnnotations and
    _computeInitialPositionAnnotations set imported=false (auto-generated
    UI display aids) (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ai-bridge.js: _buildPGNString only exports
    imported=true entries (human-authored annotations from PGN import);
    auto-generated annotations no longer pollute PGN (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/tablebase.js: importPGN sets imported=true
    on extracted [%csl]/[%cal] annotation cache entries (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 61 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - src/main/assets/chess.src/ui.js: _resetGameUIState() now centrally clears
    ALL game-related caches (_reviewEvalCache, _ecoRecCache, _pvCache,
    _pendingEngineSANs/_pendingEnginePVs, _multiPV*, reviewCritical,
    _sfEval/_sfDepth/etc, _cachedOriginalPGN, playerWhite/playerBlack,
    _setupFEN, _importedStartMoveNum, _needNewGameForEngine, _tbLoading,
    _tbRetryCount); _exitSetupImpl/_startGameImpl redundant manual clears
    removed; _pgnCacheSaveCurrent pure-import detection (saves original PGN
    text for pure imports) (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ai-bridge.js: _preMoveComment computation for
    initial-position eval annotation as separate pre-move {} comment;
    removed i===0 initial annotation block; pass preMoveComment to
    composePGN (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/pgn-standard.js: composePGN preMoveComment
    support — inserts {} comment before first move (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/tablebase.js: _applyImportedFEN + importPGN
    reordered — _resetGameUIState first, then set entry-point-specific
    values (_setupFEN, _importedStartMoveNum, playerWhite/playerBlack)
    AFTER reset so they survive (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 60 (2026.7.5, same-version revision):
  - Version remains versionCode=111, versionName="1.1.1".
  - src/main/java/com/Regalia/StockfishNative.java: writer-close paths
    (cleanupEngineResources/shutdown/cleanupFailedEngine) now use _writerLock
    (consistent with Phase 58 heartbeat path); sendUciCommand writer access
    also wrapped in _writerLock (GPL v3, DroidFish-derived).
  - src/main/java/com/Regalia/ChessWebViewClient.java: onRenderProcessGone
    adds crash-count backoff (max 3 recreates per 60s window) (AGPL v3, original).
  - src/main/assets/chess.src/ui.js: Integer_bitcount adds n>>>0 unsigned
    coercion; render-error title uses T('render_error_title') i18n
    (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/pgn-standard.js: normalizeTagValue regex adds
    \t tab filtering (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/game-logic.js: new i18n key render_error_title
    (GPL v3, DroidFish-derived).
  - src/main/assets/stats.html: new navigation buttons (⏮ ◀ ▶ ⏭) below the
    stats board with mainline/variation context-aware behavior; new i18n keys
    nav_first/nav_prev/nav_next/nav_last/nav_main_line/nav_variation;
    _exportStaticHTML removes nav buttons from FEN-only export (GPL v3,
    DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - No new third-party code introduced.

v1.1.1 Phase 59 (2026.7.5):
  - Version bumped to versionCode=111, versionName="1.1.1".
  - src/main/assets/chess.src/ai-bridge.js: _commentHasText helper, every-5-moves
    PGN annotation dedup, initial-position annotation on first move, i18n
    resign/timeout comments, batch analyze-all session state
    (_reviewAnalyzeStep/_reviewAnalyzeGen/_evalRequestBatchGen), _requestBatchEval()
    function, onEngineEval batch-gen-first + stale-callback-caching,
    requestEngineEval user-nav-only (blocks during batch) (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/ui.js: _refreshEvalTrendChart() lightweight chart
    DOM refresh, reviewAnalyzeAll/_reviewAnalyzeAdvance use _reviewAnalyzeStep
    (decoupled from reviewStep), exitReview clears batch state
    (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/game-logic.js: new i18n keys (pgn_resign_white,
    pgn_resign_black, pgn_timeout_white_wins, pgn_timeout_black_wins,
    pgn_initial_position); loading_title bumped to v1.1.1
    (GPL v3, DroidFish-derived).
  - src/main/assets/chess.src/index.html.tpl: <title> bumped to v1.1.1
    (GPL v3, DroidFish-derived).
  - src/main/assets/chess.html: rebuilt from chess.src/.
  - src/main/res/values/strings.xml: app_name = "Regalia v1.1.1" (AGPL v3, original).
  - src/main/java/com/Regalia/MainActivity.java: VERSION = "v1.1.1" (AGPL v3, original).
  - src/main/java/com/Regalia/StockfishNative.java: ENGINE_VERSION = "v1.1.1"
    (GPL v3, DroidFish-derived).
  - src/main/java/com/Regalia/ChessApp.java: init log "v1.1.1" (AGPL v3, original).
  - src/main/java/com/Regalia/ChessWebViewClient.java: version comment "v1.1.1"
    (AGPL v3, original).
  - No new third-party code introduced.

v1.1.0 Phase 58 (2026.7.5):
  - No changes to res/ files. Phase 58 adds a JS-layer feature (every-5-moves
    PGN {} eval annotation in pgn-standard.js + game-logic.js + ai-bridge.js)
    and Java-layer concurrency fixes (StockfishNative.java). strings.xml and
    all other res/ files are unaffected.

v1.1.0 Phase 57+ (2026.7.5):
  - res/README.license (AGPL v3): Line 14 strings.xml description version
    reference updated from "Regalia v1.0.8" to "Regalia v1.1.0" (LIC-2 fix to
    match the actual app_name value updated in Phase 53). Historical changelog
    entries that mention v1.0.8 (lines 163+) are preserved as accurate
    historical records.
  - strings.xml: No changes (app_name remains "Regalia v1.1.0").
  - No other res/ files changed.

v1.1.0 Phase 53 (2026.7.3):
  - strings.xml (AGPL v3): app_name "Regalia v1.0.9" → "Regalia v1.1.0".
  - No other res/ files changed.


v1.0.9 Phase 52 (2026.7.2):
  - strings.xml (AGPL v3): app_name "Regalia v1.0.8" → "Regalia v1.0.9".
  - No other res/ files changed.


v1.2.0 Phase 78 (2026.7.11):
  - strings.xml: app_name "Regalia v1.1.2" → "Regalia v1.2.1".
